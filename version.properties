# Source of truth for the released app version. Read by app/build.gradle.kts and,
# via UpdateCheckData, by F-Droid's tag-based update checker (see
# docs/f-droid/io.github.ouj4k2q5.minlauncher). Bumped by hand on the release
# branch as part of the release procedure (docs/releasing.md); the release tag
# must point at a commit that already carries this version.
versionName=0.0.3
versionCode=3
